version: '3'
services:
  cellapp:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /path to data:/app/data
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      - DATA_FOLDER=/app/data
