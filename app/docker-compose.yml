version: '3'
services:
  cellapp:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - <your_data_folder>:/data
      - ./:/app
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      - DATA_FOLDER=/data
      - NUMBA_CACHE_DIR=/tmp 
    command: supervisord
